{
  "name": "back",
  "version": "1.0.0",
  "description": "Back simulado en json-server para el consumo de react",
  "main": "database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json": "json-server --watch --port 5174 database.json" 
  },
  "keywords": [
    "sumulacion",
    "liquidacion"
  ],
  "author": "Liquidacion",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
